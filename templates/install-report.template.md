# Installation Report

## Identity

State:

Distribution version:

Source commit:

Distribution tree SHA-256:

Hermes version:

Tested semantic range: `>=0.21.0,<0.22.0`

Tested upstream build: `b51c055a`

Platform:

Profile:

Run ID:

## Requested capability packs

List requested packs. Do not present them as active.

## Resolved capability packs

List only packs with a private resolution record and completed provider setup. The profile wrapper does not resolve optional packs, so an empty list is expected before AI-led onboarding.

## Installed assets

Record every distribution-owned path confirmed in the installed profile.

## Dependencies

Hermes compatibility:

Python compatibility:

Missing dependencies:

## Credential gates

List provider authorizations still needed. Never include a secret value.

## Verification

Record each check, result, time, target, and nonsecret evidence.

## Warnings

List limitations and unfinished configuration.

## Backup and rollback

Pre-update backup:

Checksum:

Profile fingerprint:

Rollback point:

Rollback tested:

## Final rule

Only `verified` means the selected installation is complete. `planned`, `dependency_blocked`, `credential_blocked`, `verification_blocked`, `failed`, and `rolled_back` describe other honest states.
