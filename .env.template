# This file is an inventory of optional variable names, not a populated secret file.
# Hermes installs it as .env.EXAMPLE. Prefer Hermes setup, model, auth, gateway,
# MCP, or an approved secret manager. Never commit a filled copy.

# Optional Secure Credentials reference utility
SECURE_CREDENTIALS_BASE_URL=
SECURE_CREDENTIALS_PREFIX=
SECURE_CREDENTIALS_DROP_DB=
SECURE_CREDENTIALS_VAULT=
SECURE_CREDENTIALS_KEY_FILE=
SECURE_CREDENTIALS_OUTBOX=
SECURE_CREDENTIALS_EXPOSURE=
SECURE_CREDENTIALS_RATE_LIMIT=

# Provider and channel variables are added only after private onboarding.
# Do not place OAuth refresh credentials in this public template.
